rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {  
    // Anyone can READ anything in the `/users` collection and subcollections.
    match /users/{document=**} {
      allow get, list:
      if true;
    }

    // Anyone can READ anything in the `/teams` collection and subcollections.
    match /teams/{document=**} {
      allow get, list:
      if true;
    }
    
    // UPDATE: Must be authenticated.
    // Can only update their own.
    // Can only update firstname, lastname, and team.
    match /users/{uid}{
      allow update:
      if request.auth != null &&
      request.auth.uid == uid &&
      request.resource.data.diff(resource.data).affectedKeys().hasOnly(['firstname', 'lastname', 'team'])
    }
  }
}